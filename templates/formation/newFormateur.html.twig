{% extends 'base.html.twig' %}

{% block title %}Ajoutez une nouveau formateur{% endblock %}

{% block body %}
<div class="content">
    {% if edit %}
        <h1>Editer un formateur existant</h1>
    {% else %}
        <h1>Ajoutez un nouveau formateur</h1>
    {% endif %}

    <div class="card">
        {{ form_start(formAddFormateur) }}

        
        
        {{ form_end(formAddFormateur) }}
    </div>
</div>

{% endblock %}