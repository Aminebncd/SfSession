{% extends 'base.html.twig' %}

{% block title %}Ajoutez une nouvelle formation{% endblock %}

{% block body %}

<div class="content">
    {% if edit %}
        <h1>Editer une formation existante</h1>
    {% else %}
        <h1>Ajoutez une nouvelle formation</h1>
    {% endif %}
    {{ form_start(formAddFormation) }}
    <div class="card">

    </div>
    {{ form_end(formAddFormation) }}
</div>

{% endblock %}